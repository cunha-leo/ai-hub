# ai-hub — Handoff da sessão (16/08/2026)

> **Propósito:** transportar o contexto desta sessão (Copilot local) para o ambiente remoto da VPS. Qualquer IA/agente que ler este arquivo continua o trabalho do ai-hub com o estado completo. **Não contém segredos** (chaves, senhas).
>
> **Origem da sessão:** transcript local do Copilot (JSONL) de 16/08/2026 — este documento é a versão portátil e limpa.

---

## 1. O que foi feito hoje (16/08/2026)

1. **DeepSeek Harness (dsh) na VPS** — deploy completo no EasyPanel (source Dockerfile), com proxy HTTP+WebSocket (fix do 403 "Host fence" e das sessões "Ungrouped"), volume persistente (`/root/dsh-data` → `/root/.dsh`) e `ripgrep`.
2. **Authelia (login)** — 2FA (TOTP + passkey) e recuperação de senha por e-mail (SMTP Hostinger `submissions://…:465`), com forward Gmail de fallback.
3. **Traefik** — `dsh.yaml` com forward-auth nos domínios `deepseek.upexflow.com` e `authai.upexflow.com`.
4. **Chaves DeepSeek por canal** — `deepseek-harness` / `vscode-extension` / `copilot` / `upexnote` ativas; `deepseek-agent` **revogada** (terminal usa sessão de navegador, não API).
5. **Telemetria** — levantado: 30 dias ≈ US$ 4,82 · 944 req · 229,4M tokens (~US$ 0,02/M efetivo). Registrado na seção 12 do doc MD do Drive.
6. **Documentação** — dossiê DOCX v1.1 + doc MD v1.2 no Google Drive; orquestração documental no repo (commit `a112bcd`).
7. **Ambiente remoto** — `~/.ssh/config` criado (host `upexflow-vps`), Remote-SSH instalado, repo `ai-hub` clonado em `/root/ai-hub`, extensões instaladas na VPS (Python/Pylance/YAML/Prettier/Copilot), chaves DeepSeek das extensões configuradas e testadas (responderam "ok").

---

## 2. Estado atual (referência rápida)

| Item | Estado |
|---|---|
| Harness (dsh) | ✅ rodando 24/7 na VPS, com login + 2FA |
| Authelia | ✅ 2FA + reset por e-mail + forward Gmail |
| Chaves | ✅ 4 ativas por canal (telemetria) |
| Repo `ai-hub` | ✅ local + GitHub (`cunha-leo/upexflow-ai-hub`) + VPS (`/root/ai-hub`) |
| Remote-SSH | ✅ `upexflow-vps` funcionando, extensões + chaves ativas |
| Documentação | ✅ Drive (DOCX v1.1 / MD v1.2) + repo (docs/) |

---

## 3. Decisões de arquitetura vigentes

- **VPS = execução/casa** (ai-hub 100% + futuros projetos web). **Local = cofre de mídia** + upexnote.
- **upexnote = LOCAL-FIRST** (desktop, Windows/Tauri) — **não** clona na VPS.
- **GitHub = ponte** entre local e VPS (pull antes de editar, push ao terminar).
- **Chaves por canal** para telemetria consistente.
- **Reuso de plataforma:** Authelia = SSO único; LiteLLM (futuro) = gateway de modelos.

---

## 4. Onde está cada coisa

- **Repo local:** `C:\Users\cunha\Projects\upexflow\ai-hub`
- **Repo na VPS:** `/root/ai-hub`
- **GitHub:** `cunha-leo/upexflow-ai-hub` (privado, `main`)
- **VPS:** `root@vps.upexflow.com` (SSH `upexflow-vps` / chave `~/.ssh/upexnote_vps`)
- **Documentação no Drive:** `...\UpexFlow\ai-hub\DeepSeekHarness` (DOCX + MD)
- **Configs reais na VPS:** `/root/authelia/config`, `/root/.secrets`

---

## 5. Pendências / próximos passos

1. **[Opcional] LiteLLM na VPS** — gateway multi-provedor (OpenAI/Claude/Gemini) por 1 endpoint OpenAI-compatível; privado (self-hosted).
2. **[Opcional] Tema custom do Authelia** — logo UpexFlow + CSS (`resources/themes/<nome>` + `theme:`).
3. **[Opcional] Multi-provedor no Harness** — visão via modelo com visão (GPT-5/Claude/Qwen-VL) na mesma sessão.
4. **Metrificação semanal** de telemetria (Export do DeepSeek Platform).

---

## 6. Como continuar (para a IA remota)

1. Leia `AGENTS.md` (raiz) e `docs/CONTEXT_ORCHESTRATION.md` (porta de entrada).
2. Leia `docs/PROJECT_CONTEXT.md` e `docs/FEATURE_VALIDATION_AND_ROADMAP.md`.
3. Confira `git status`, `git log`, `git branch -vv` antes de assumir conteúdo canônico.
4. Trabalhe em `/root/ai-hub`, com `git pull` antes de editar e `git push` ao terminar.

---

*Handoff gerado em 16/08/2026 a partir da sessão local do Copilot — para continuidade no ambiente remoto da VPS.*
